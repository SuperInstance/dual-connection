[package]
name = "dual-connection"
version = "0.1.0"
edition = "2024"
description = "Dual affine connections (e-connection, m-connection) on statistical manifolds"
license = "MIT OR Apache-2.0"
repository = "https://github.com/SuperInstance/dual-connection"
keywords = ["information-geometry", "statistical-manifold", "dual-connection", "affine-connection", "exponential-family"]
categories = ["mathematics", "science"]

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
